---
type: showroom

default_width: 35
persist_url_state: true

view_switcher:
  enabled: true
  default_mode: split

tabs:
  # Terminal for oc and curl commands
  - name: Terminal
    path: /wetty
    port: 443

  # RHOAI Dashboard — visual model serving management
  - name: RHOAI Dashboard
    url: 'https://rhods-dashboard-redhat-ods-applications.${DOMAIN}'

  # OpenShift Console
  - name: OCP Console
    url: 'https://console-openshift-console.${DOMAIN}'
