apiVersion: v1
kind: Namespace
metadata:
  name: triforce
  labels:
    app.kubernetes.io/part-of: triforce
    intel.com/hardware: xeon-6767p
---
# Secret created manually at deploy time:
#   oc create secret generic triforce-secrets -n triforce \
#     --from-literal=postgres-user=triforce \
#     --from-literal=postgres-password="$(openssl rand -base64 16)" \
#     --from-literal=litellm-api-key="$(openssl rand -hex 16)"
