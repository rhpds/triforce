name: modules
title: "Triforce — AI Inference at Scale"
version: ~
nav:
  - modules/ROOT/nav.adoc
asciidoc:
  attributes:
    user: "{user}"
    password: "{password}"
    openshift_api_url: "{openshift_api_url}"
    openshift_console_url: "{openshift_console_url}"
    openshift_cluster_ingress_domain: "{openshift_cluster_ingress_domain}"
    frontend_url: "https://triforce-{user}.{openshift_cluster_ingress_domain}"
    healthcare_url: "https://healthcare-{user}.{openshift_cluster_ingress_domain}"
    finserv_url: "https://finserv-{user}.{openshift_cluster_ingress_domain}"
    orchestrator_url: "https://orchestrator-{user}.{openshift_cluster_ingress_domain}"
