= Explore: Disaggregated Inference (llm-d)
:navtitle: llm-d Inference

*Time: ~10 minutes* · *Role: ML Engineer, Performance Engineer*

== What You'll Learn

llm-d separates the two phases of LLM inference — prefill (compute-heavy) and decode (memory-bound) — across specialized nodes. An SLO-based planner routes requests to nodes with capacity.

== How It Works

Standard inference:
```
One node does everything: prompt processing + token generation + queueing
```

Disaggregated inference (llm-d):
```
Request → llm-d Planner (SLO: 2s latency target)
              │
    ┌─────────┼─────────┐
    │                   │
Prefill Node        Decode Node
(compute-heavy)     (memory-bound)
(128 cores)         (stream tokens)
    200ms               1.2s
              │
         Total: 1.4s (within 2s SLO)
```

== Current Status

[cols="1,1,2",options="header"]
|===
| Component | Status | Notes

| llm-d planner | Roadmap | Requires RHAIIS + KServe on cluster
| vLLM pools | Roadmap | Prefill pool + decode pool configuration
| Inference Gateway | Manifests written | infrastructure/llm-d/ in the repo
| SLO routing | Roadmap | Planner makes routing decisions based on latency targets
|===

== Exercise: Review the Manifests

[source,bash,role="execute"]
----
ls /opt/triforce/infrastructure/llm-d/ 2>/dev/null || echo "llm-d manifests available in the repo at infrastructure/llm-d/"
----

The manifests define InferenceGateway + vLLM pool resources. They're ready to deploy when RHAIIS is available on the cluster.

== The Takeaway

Beyond identical replicas — llm-d specializes nodes for different phases of inference. The prefill burst goes to high-core nodes. Token streaming goes to memory-optimized nodes. The planner routes based on latency SLO targets.

This is the future of CPU inference at scale. Today's 7.8s pipeline → 1.4s with full disaggregation. Same $0/token.

[.next-nav]
xref:99-conclusion.adoc[Back to module list →]
